services:
  rails:
    port: 3000
    runtime: rails
    environment:
      DEVELOPMENT: true
    commands: 
      - bundle package
      - bundle install --local
      - yarn --verify-tree
      - bundle exec rails s
