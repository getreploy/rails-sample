services:
  rails:
    port: 3000
    runtime: rails
    environment:
      DEVELOPMENT: true
    commands: 
      - bundle install --path=cache
      - yarn --verify-tree
      - bundle exec rails s
